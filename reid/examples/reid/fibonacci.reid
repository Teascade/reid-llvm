// Main
fn main() -> bool {
    return 5 == fibonacci(5);
}

// Fibonacci
fn fibonacci(value: u16) -> u16 {
    if value <= 2 {
        return 1;
    } else {
        return fibonacci(value - 1) + fibonacci(value - 2);
    };
}
