// Main
fn main() -> bool {
    return (5 == fibonacci(5)) && false;
}

// Fibonacci
fn fibonacci(value: u16) -> u16 {
    let ret = if value <= 2 {
        1
    } else {
        fibonacci(value - 1) + fibonacci(value - 2)
    };
    ret
}
