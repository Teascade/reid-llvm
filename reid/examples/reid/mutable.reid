// Arithmetic, function calls and imports!

fn indirection() -> bool {
    return true;
}

fn main() -> u16 {
    let mut test = 5;
    let addition = 10;

    if indirection() {
        test = 11;
    }

    return test + addition;
}
